# ==========================================
# Tapio golangci-lint Configuration v1.x/v2.x
# ==========================================

run:
  timeout: 5m
  issues-exit-code: 1
  tests: true
  build-tags:
    - ebpf
  modules-download-mode: readonly
  skip-dirs:
    - internal/observers/network/bpf  # Generated eBPF code (bpf2go output)

linters:
  enable:
    # Essential linters only - no formatters
    - errcheck          # Check for unchecked errors
    - gosimple          # Simplify code
    - govet             # Go vet examination
    - ineffassign       # Detect ineffectual assignments
    - staticcheck       # Go static analysis
    - unused            # Check for unused constants, variables, functions and types

# ==========================================
# Issues Configuration
# ==========================================
issues:
  # Set to 0 to disable limit
  max-issues-per-linter: 50
  max-same-issues: 3

  # Show only new issues if `revive` is enabled
  new: false

  # Fix found issues (if it's supported by the linter)
  fix: false